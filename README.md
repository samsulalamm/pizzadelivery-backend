To install the dependency, run the following command in the project root directory:

### `composer update`

## Database creation
Create a database in Mysql and configure in the .env file and run the following command:

### `php artisan migrate`
